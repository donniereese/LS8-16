// const args = process.args.slice(1);
//
// read ARGS
// error if wrong lenght
// look for typ of hex or binary
// look for file at supplied path
// error if it doesn't exist
// split into Array
// clean everything except correct digits, clean # and everything after

const Manager = require('./manager')
const baseInst = require('../../conf/instDef')

const Dictionary = new Manager()

for (const [key, value] of Object.entries(animals)) dict.insert(key, value.binary, value.description)

module.exports = Dictionary;
